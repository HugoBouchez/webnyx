# 🚀 Lancer le Site Web

## Méthode 1 : Double-clic (Recommandé pour Windows)

**Double-cliquez sur le fichier `lancer-site.bat`** dans l'explorateur Windows.

Le script va :
- ✅ Vérifier et installer automatiquement les dépendances si nécessaire
- ✅ Lancer le serveur de développement
- ✅ Ouvrir le site sur http://localhost:3000

## Méthode 2 : Ligne de commande

Ouvrez un terminal dans le dossier du projet et exécutez :

```bash
npm run dev
```

## Méthode 3 : PowerShell

Si vous préférez PowerShell, double-cliquez sur `lancer-site.ps1` ou exécutez :

```powershell
.\lancer-site.ps1
```

---

**Note :** Le site sera accessible sur **http://localhost:3000** une fois le serveur démarré.

Pour arrêter le serveur, appuyez sur `Ctrl+C` dans le terminal.

